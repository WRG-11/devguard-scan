# SYNTHETIC fixture (Pattern 34) — fake values only, no real credentials.
# Covers DEFAULT_INCLUDE's `**/*.cfg` (setup.cfg / tox.cfg / alembic.cfg),
# added upstream and missing from this port until 2026-07-29.
[database]
password = "hunter2hunter2"
